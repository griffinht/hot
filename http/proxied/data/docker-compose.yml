networks:
  internal:
    driver: bridge
    internal: true
volumes:
  data:
    external: true
services:
  gossa:
    image: pldubouilh/gossa:v0.2.1
    restart: unless-stopped
    networks:
      - internal
    volumes:
      - data:/shared
    environment:
      PREFIX: "/gossa/"
