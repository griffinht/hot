networks:
  internal:
    driver: bridge
    internal: true
volumes:
  data:
    external: true
services:
  filebrowser:
    build: filebrowser
    restart: unless-stopped
    networks:
      - internal
    volumes:
      - data:/data
  gossa:
    image: pldubouilh/gossa
    restart: unless-stopped
    networks:
      - internal
    volumes:
      - data:/shared
    environment:
      PREFIX: "/gossa/"
