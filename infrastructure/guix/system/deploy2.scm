(list
  (car (load "cloudtest/deploy.scm"))
  (car (load "griffinht/deploy.scm"))
  (car (load "guix/deploy.scm"))
  (car (load "hot/deploy.scm"))
  (car (load "hot-data/deploy.scm"))
  (car (load "hypervisor/deploy.scm"))
  (car (load "nerd-vps/deploy.scm")))
