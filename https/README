development:
docker compose up nginx

might need to tweak RESOLVER to 127.0.0.11 if you are using docker instead of podman

http is available at http://localhost:8000 but currently it just returns 404 always
then connect to https://localhost:4430 and accept the self signed cert
